    \chapter{Provas de Conceito e Validação}

    \section{Validação do \textit{Proof of Concept} da OmniAI Gateway}
    A validação principal da arquitetura foi efetuada através da aplicação \texttt{OmniAiGateway}, nomeadamente a instanciação de uma \textbf{OmniAiGateway} como um \textit{Proof of Concept} (PoC) que integra o \textbf{OmniAI SDK} via \textit{composite build}, conforme detalhado anteriormente. Esta aplicação é responsável por carregar o ficheiro de configuração \texttt{application.conf}, resolver as variáveis de ambiente necessárias, instanciar o \texttt{KtorHttpTransportClient} e configurar os respetivos \textit{outbounds}. Adicionalmente, assegura a ativação dos módulos de segurança e telemetria, a inicialização do \textit{runtime} do \textit{Software Development Kit} e a exposição de \textit{endpoints} \textbf{HTTP} compatíveis com as APIs da \textbf{OpenAI} \cite{openai_api}, \textbf{Anthropic} \cite{anthropic_api} e \textbf{Gemini} \cite{google_gemini_api}.
    
    \begin{figure}[H]
        \centering
        \begin{tikzpicture}[
        % node distance: vertical, horizontal
            node distance=0.8cm and 1.5cm,
        % Estilo uniforme para as caixas de cliente e fornecedores
            box/.style={rectangle, draw, rounded corners, align=center, text width=4.0cm, minimum height=1.4cm, inner sep=5pt, thick},
        % Estilo para a \textit{gateway} (maior e com preenchimento)
            gateway/.style={rectangle, draw, rounded corners, align=center, text width=4.5cm, minimum height=2.5cm, inner sep=6pt, fill=gray!15, thick},
            arrow/.style={-Latex, thick}
        ]
            % --- COLUNA ESQUERDA ---
            \node[box] (rest) {REST Client};
            \node[box, below=0.8cm of rest] (claude) {\textbf{Claude Code}\\ \texttt{ANTHROPIC\_BASE\_URL}};
            \node[box, below=0.8cm of claude] (node) {\textbf{SDK} \textbf{Anthropic}\\ \textbf{Node.js}};

            % --- GATEWAY CENTRAL ---
            \node[gateway, right=of claude] (gateway) {\textbf{OmniAI Gateway}};

            % --- COLUNA DIREITA ---
            % Ajuste de yshift para espaçamento vertical no lado direito
            \node[box, right=of gateway, yshift=1.2cm] (providers) {Fornecedor configurado\\ \textbf{Gemini}, \textbf{OpenAI}\\ \textbf{Anthropic}};
            \node[box, right=of gateway, yshift=-1.2cm] (obs) {\textbf{Prometheus} + \textbf{Grafana}\\ via \textbf{OpenTelemetry Collector}};

            % --- LIGAÇÕES ---
            \draw[arrow] (rest.east) -- (gateway.west);
            \draw[arrow] (claude.east) -- (gateway.west);
            \draw[arrow] (node.east) -- (gateway.west);

            \draw[arrow] (gateway.east) -- (providers.west);
            \draw[arrow] (gateway.east) -- (obs.west);
        \end{tikzpicture}
        \caption{Cenários de validação do \textit{Proof of Concept}}
        \label{fig:poc-validation}
    \end{figure}

    \section{Validação com REST Client}
    O ficheiro \texttt{gateway-requests.http} foi utilizado para a validação manual dos \textit{endpoints} expostos pela \textbf{OmniAi Gateway}. Este ficheiro agrupa pedidos direcionados a \texttt{/v1/chat/completions}, \texttt{/v1/messages} e \texttt{/v1beta/models/{model}:generateContent}, cobrindo cenários de respostas unárias, \textit{streaming}, geração baseada em parâmetros e integração com autenticação via token Bearer.

    A relevância deste cenário de testes fundamenta-se em dois pilares principais. Em primeiro lugar, viabilizou a validação dos contratos \textbf{HTTP} recebidos pela \textbf{OmniAI Gateway}, prescindindo do acoplamento a aplicações externas. Em segundo lugar, proporcionou a testagem rápida e integral do fluxo de dados, desde a emissão do token pelo \textbf{Logto} e a respetiva autenticação do pedido, passando pela tradução \textit{inbound}, conversão para o \textit{outbound} e invocação do fornecedor configurado, até à devolução da resposta no formato esperado pelo cliente.

    \section{Validação de Segurança com Logto}
    A camada de segurança foi validada com \textbf{Logto} como \textit{Authorization Server} local. O ambiente \textbf{Docker} inclui \textbf{Logto} \cite{logto_docs}, \textbf{PostgreSQL} \cite{postgresql_ref}, \textbf{OpenTelemetry Collector} \cite{opentelemetry_otlp}, \textbf{Prometheus} \cite{prometheus_ref} e \textbf{Grafana} \cite{grafana_ref}. A configuração do \textit{Proof of Concept} usa \textbf{OIDC} Discovery \cite{oidc_discovery} para obter metadados do servidor de autorização e validar \textit{tokens} com audiência configurada para o recurso da API.

    Durante o desenvolvimento foram realizados testes usando o \textbf{Keycloak} \cite{keycloak_docs} como servidor de autorização externo, mas a configuração final mudou para usar o \textbf{Logto} \cite{logto_docs}. O \textbf{Logto} facilitou a criação de \textit{API Resources} e de aplicações \textit{Machine-to-Machine} (M2M). Estas aplicações M2M utilizam o fluxo \textit{Client Credentials Grant} \cite{ref_ccG}, que permite que sistemas se autentiquem e comuniquem de forma automática com a \textbf{OmniAI Gateway} utilizando as suas credenciais. Utilizar o \textbf{Logto} permitiu também a configuração de fluxos \textit{OIDC - OpenID Connect} \cite{ref_oidc} adequados à validação da \textbf{OmniAI Gateway} como \textit{Resource Server}\cite{resource_ref}.


    \section{Validação de Observabilidade}
    A observabilidade foi validada com \textbf{OpenTelemetry Collector}, \textbf{Prometheus} e \textbf{Grafana}. A \textbf{OmniAiGateway} ativa métricas na DSL do \textbf{OmniAI SDK}, incluindo latência padrão, contagem de \textit{tokens} e contagem de erros. Também acrescenta atributos como IP do cliente, versão do \textbf{OmniAI SDK}, audiência e dados derivados do resultado de autenticação, como \texttt{sub}, \texttt{email} ou \texttt{preferred\_username} quando disponíveis.

    O objetivo desta validação foi confirmar que a \textbf{OmniAI Gateway} consegue produzir telemetria e garantir observabilidade em tempo real durante o processamento de pedidos. A utilização de \textbf{Prometheus} \cite{prometheus_ref} e \textbf{Grafana} \cite{grafana_ref} permitiu monitorizar as métricas operacionais da aplicação, comprovando que o módulo de telemetria corre de forma isolada da lógica de inferência sendo definido via DSL e exposto por \textit{adapters} dedicados.


    \section{Validação com Aplicações Cliente}

    Para validar a compatibilidade da \textbf{OmniAI Gateway} com aplicações existentes, foram estudados e testados clientes que permitem configurar \textit{endpoints} alternativos para APIs de inferência. Esta capacidade é particularmente relevante porque permite introduzir a \textbf{Gateway} numa aplicação já existente sem necessidade de modificar a sua lógica interna.

    Um dos casos analisados foi o \textbf{Claude Code}, que disponibiliza a variável de ambiente \texttt{ANTHROPIC\_\allowbreak BASE\_\allowbreak URL} para redefinir o \textit{endpoint} utilizado nos pedidos à API da \textbf{Anthropic}. Para complementar esta validação, foi também desenvolvido um teste utilizando o \textbf{SDK} oficial da \textbf{Anthropic}, implementado em \texttt{node-tests/anthropicSDKTest.js}. Neste cenário, o cliente foi configurado com um \texttt{baseURL} local e uma chave fictícia, demonstrando que aplicações construídas sobre o \textbf{SDK} conseguem comunicar com a \textbf{OmniAI Gateway} quando esta expõe um contrato compatível com a API original.

    \subsection{Execução do Claude Code com autenticação utilizando o Logto}

    Para além dos testes realizados através de pedidos diretos e do \textbf{SDK} oficial, foi desenvolvido o script \texttt{run\_claude.py} com o objetivo de validar um cenário de utilização mais próximo de um ambiente real. Este script automatiza a configuração necessária para que o \textbf{Claude Code} utilize a \textbf{OmniAI Gateway} como ponto de entrada para os pedidos de inferência, sem qualquer alteração ao código do cliente.

    O processo inicia-se com a autenticação através do \textbf{Logto}. Durante a execução, o script gera os parâmetros necessários para o fluxo \textbf{PKCE}, abre o navegador para o processo de autenticação \textbf{OIDC} e disponibiliza temporariamente um servidor \textbf{HTTP} local em \texttt{localhost:8080} para receber o código de autorização devolvido pelo fornecedor de identidade. Após a autenticação do utilizador, o código obtido é trocado por um \textit{access token} associado ao recurso da API configurado no \textbf{Logto}.

    Concluída a autenticação, o script prepara automaticamente o ambiente de execução do \textbf{Claude Code}. Para tal, define uma chave fictícia em \texttt{ANTHROPIC\_API\_KEY}, configura a variável \texttt{ANTHROPIC\_\allowbreak BASE\_\allowbreak URL}para apontar para a instância local da \textbf{Gateway} e injeta o \textit{access token} obtido através do cabeçalho \texttt{Authorization}, utilizando a variável \texttt{ANTHROPIC\_CUSTOM\_HEADERS}. Finalmente, o comando \texttt{claude} é iniciado já com toda a configuração necessária.

    Esta validação permitiu demonstrar que uma aplicação desenvolvida para comunicar com a API da \textbf{Anthropic} pode ser integrada com a \textbf{OmniAI Gateway} apenas através de configuração. O cenário exercita simultaneamente a autenticação através do \textbf{Logto}, a validação de \textit{tokens} na \textbf{Gateway} e o encaminhamento dos pedidos para os fornecedores configurados, constituindo assim uma validação de ponta a ponta da arquitetura proposta.

    Para além do \textbf{Claude Code}, foram ainda analisadas plataformas como \textit{OpenCode} \cite{opencode_ref}, \textit{LibreChat} \cite{librechat_ref} e \textit{Dify} \cite{dify_ref}, uma vez que também suportam a configuração de \textit{endpoints} compatíveis com APIs existentes. Estes casos reforçam uma das principais vantagens da arquitetura proposta, que quando uma aplicação já utiliza uma interface amplamente adotada, a introdução da \textbf{OmniAI Gateway} pode ser realizada através de configuração, evitando alterações na lógica da aplicação cliente e reduzindo significativamente o esforço de integração.

    \section{Testes}

    A testabilidade foi fundamental no desenvolvimento do \textbf{OmniAI SDK}, sendo utilizada para validar o funcionamento individual de cada módulo que o constitui. A arquitetura em modulos facilitou a realização de testes, tornando possível isolar e testar o comportamento de cada componente sem a necessidade de instanciar o sistema por completo.

    Um exemplo desta testabilidade verificou-se na validação do modulo dos \textbf{contratos (\texttt{contracts})}. A estratégia de testes para este módulo consistiu em instanciar os SDKs oficiais dos fornecedores (\textit{OpenAI}, \textit{Anthropic}, \textit{Gemini}) num ambiente de testes e capturar os pedidos gerados através de um servidor HTTP simulado (\textit{mock server}). Os \textit{payloads} intercetados foram, posteriormente, desserializados através dos DTOs do \textbf{OmniAI SDK}. Estes testes permitiram validar diretamente o mapeamento dos nossos DTOs face às respostas das \textit{APIs} oficiais, prevenindo erros de conversão de dados sem a necessidade de integrar os restantes componentes do sistema.

    Assim, a arquitetura do projeto foi pensada para facilitar a criação de testes isolados. Isto permitiu validar cada componente individualmente como já foi referido, garantindo assim o correto funcionamento de todo o sistema antes de juntar toda as peças, previnindo problemas de integração e aumentando a confiabilidade do \textbf{OmniAI SDK}.